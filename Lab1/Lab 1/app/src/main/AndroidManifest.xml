<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Week1"
        tools:targetApi="31">

        <!-- Main Activity -->
        <activity
            android:name="com.example.aboutme.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- About Me Activity -->
        <activity
            android:name="com.example.aboutme.AboutMeActivity"
            android:exported="false"/>

        <!-- Projects Activity -->
        <activity
            android:name="com.example.aboutme.ProjectsActivity"
            android:exported="false"/>

        <!-- Contact Activity -->
        <activity
            android:name="com.example.aboutme.ContactActivity"
            android:exported="false"/>

    </application>
</manifest>